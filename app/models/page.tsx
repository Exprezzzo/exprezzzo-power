"use client";import catalog from"@/lib/providers/catalog.json";export default function ModelsPage(){return(<div className="min-h-screen p-8 bg-bgDark dark:bg-bgLight text-textDark dark:text-textLight"><h1 className="text-4xl font-bold mb-8">Model Directory</h1><div className="grid grid-cols-3 gap-6">{catalog.providers.map(provider=>(<div key={provider.id}className="p-6 bg-surfaceDark dark:bg-surfaceLight rounded-2xl"><h3 className="text-xl font-bold mb-2">{provider.name}</h3><span className={`px-3 py-1 rounded-full text-sm ${provider.status==="available"?"bg-green-500/20 text-green-400":"bg-gray-500/20 text-gray-400"}`}>{provider.status==="available"?"Available":"Coming Soon"}</span>{provider.models&&(<ul className="mt-4 space-y-2">{provider.models.map(model=>(<li key={model.id}className="text-sm">{model.id}<span className="ml-2 text-xs text-gray-500">{model.caps?.join(", ")}</span></li>))}</ul>)}</div>))}</div></div>);}
